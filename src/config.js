// Public configuration only. Never put API keys or invitation codes here.
// After deployment, set this to the actual HTTPS origin from your hosting dashboard.
// Empty: same-origin for local/hosted app; GitHub Pages asks the owner to connect a service.
globalThis.MelorinaConfig={apiBaseUrl:''};
