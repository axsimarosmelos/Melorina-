{
  "name": "melorina",
  "version": "0.4.0",
  "private": true,
  "description": "An adaptive Emirati Arabic conversation prototype",
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "start:voice": "node --env-file=.env scripts/serve.js"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
